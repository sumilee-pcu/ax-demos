# PRD: PDF 에디터를 "파일 스튜디오"로 확장

- 대상 저장소: `sumilee-pcu/ax-demos` (로컬: `/Users/sumilee/2026/ax-demos`)
- 기반 파일: `pdf.html` (pdf-lib 1.17.1 + pdf.js 3.11.174, 단일 HTML, 100% 브라우저 처리)
- 벤치마크: 딸깍 스튜디오 (https://dalkak-studio.vercel.app): 이미지 6종 + PDF 4종 + 문서·텍스트 3종
- 작성일: 2026-07-19
- 상태: **Phase 0~4 구현·배포 완료(2026-07-19)**: 13종 전 도구 라이브(https://ax-demos.vercel.app/studio/). 게이트 G0~G4는 자동 검증 통과, 최종 판정은 사용자 몫
- 구현 노트: (1) pdf.js 렌더는 `intent:"print"` 사용(백그라운드 탭 rAF 정지 대응) (2) 테스트 픽스처 동봉(7장)은 미이행: 검증은 브라우저 내 생성 픽스처로 대체
- **T11 URL 단축 해결기(2026-07-20)**: 당초 계획한 qrlink API는 폐기. 조사 결과 qrlink.vercel.app은 sumilee-pcu/qrlink(Vite) 저장소가 아니라 **무관한 옛 create-react-app 배포**여서 shorten 함수가 애초에 없었고, 그 repo에 아무리 푸시해도 그 도메인엔 반영 안 됨(프로브 파일로 확정). 대신 키·백엔드 없이 CORS를 허용하는 공개 단축 서비스로 실제 동작 구현: 추적 없는 직접 301인 **is.gd → v.gd** 우선(공문용), 둘은 동일 백엔드라 동반 장애 가능하므로 그때만 **TinyURL** 최후 폴백(제휴 리다이렉터 경유 가능 → 사용자에게 안내·재시도 권고), 셋 다 실패 시 QR만. is.gd 정상 시 clean 301 리다이렉트 실측 확인(`is.gd/q7x9eD`→원본). QR은 항상 클라이언트 생성. 단축은 원리상 서버 전송이 불가피하므로 이 항목만 "외부 전송 있음"을 UI에 명시
- 제품명: **내PC 스튜디오**: "파일이 내 PC를 떠나지 않습니다" (2026-07-19 확정)
  - 공공기관 소구 키워드를 브랜딩 축으로 사용: 무유출·무설치·무결재·무료
  - 도구 라벨에 행정 어휘 병기: 예) PDF 병합: 붙임파일 하나로, 이미지 압축: 제출용 용량 줄이기

---

## 1. 배경과 목표

### 1.1 현재 상태

`pdf.html`은 다음을 이미 갖추고 있다.

- 여러 PDF 불러오기(버튼 + 드래그앤드롭), 페이지 썸네일 렌더(pdf.js)
- 페이지 삭제 / 좌우 회전 / 순서 이동
- 전체를 하나로 병합해 `edited.pdf` 다운로드(pdf-lib)
- 서버 전송 없음(전 과정 브라우저 메모리 내 처리)

즉 딸깍 스튜디오의 "PDF 병합" + 페이지 편집은 이미 보유했고, 부족한 것은
**이미지 도구군 전체, PDF 입출력 변환(이미지↔PDF, 분할·추출), 문서·텍스트 도구군, 그리고 여러 도구를 담는 공통 셸(사이드바·통계·테마·언어)** 이다.

### 1.2 목표

- 딸깍 스튜디오와 동급의 13종 도구를 **업로드 없는 클라이언트 처리** 원칙 그대로 제공한다.
- ax-demos의 정체성(노빌드 정적 HTML, Vercel 즉시 배포, 바이브코딩 강의 데모)을 유지한다.
- 기존 `pdf.html`의 다크 프로 디자인 토큰(`--bg`, `--grad`, Pretendard + JetBrains Mono)을 전체 도구에 일관 적용한다.

### 1.3 비목표

- 회원가입·로그인·파일 서버 저장 (원칙 위반)
- PDF 본문 텍스트 편집·주석·OCR (범위 밖, 후속 검토)
- HEIC/RAW 등 특수 이미지 입력 (브라우저 네이티브 미지원, 후속 검토)
- 네이티브 앱 포장 (CapturePDFPro와 역할 분리 유지)

---

## 2. 실현 가능성 판정

결론: **13종 중 12종은 외부 서버 없이 브라우저 API + CDN 라이브러리만으로 구현 가능. URL 단축 1종만 백엔드가 필요하며, 이미 운영 중인 qrlink-studio(Vercel API + Supabase)를 재사용하면 신규 인프라 없이 해결된다.**

| # | 도구 | 구현 기술 | 판정 | 비고 |
|---|------|-----------|------|------|
| 1 | 이미지 리사이즈 | Canvas `drawImage` 스케일 | 가능 | 퍼센트/픽셀, 비율 고정 |
| 2 | 이미지 압축 | `canvas.toBlob(type, quality)` | 가능 | JPG/WEBP 품질 슬라이더 |
| 3 | 형식 변환 (JPG/PNG/WEBP) | `canvas.toBlob` | 가능(주의) | Safari는 WEBP 인코딩 미지원 → 폴백 필요(6.3) |
| 4 | 자르기 | Canvas + 드래그 크롭 UI | 가능 | 자유 비율 + 프리셋(1:1, 4:3, 16:9) |
| 5 | 회전·뒤집기 | Canvas transform | 가능 | 90° 단위 + 좌우/상하 반전 |
| 6 | 보정·필터 | `ctx.filter` 또는 픽셀 연산 | 가능(주의) | 밝기·대비·채도·흑백·세피아. 구형 Safari 폴백(6.3) |
| 7 | 이미지 → PDF | pdf-lib `embedJpg/embedPng` | 가능 | 용지 크기(A4/원본), 여러 장 순서 지정 |
| 8 | PDF 병합 | pdf-lib `copyPages` | **완료** | 현행 pdf.html 그대로 |
| 9 | PDF 분할·추출 | pdf-lib `copyPages` 부분 선택 | 가능 | 범위 문법(1-3,5) + 썸네일 클릭 선택 |
| 10 | PDF → 이미지 | pdf.js 렌더 → `canvas.toBlob` | 가능 | 배율(1x/2x/3x), 다중 페이지는 zip(JSZip) |
| 11 | URL 단축 | qrlink-studio API + Supabase | 가능(연동) | 유일한 서버 의존. CORS 허용 추가 필요 |
| 12 | JSON ⇄ CSV | 순수 JS (PapaParse CDN) | 가능 | 중첩 JSON은 dot-notation 평탄화 |
| 13 | Markdown → HTML | marked CDN + DOMPurify | 가능 | 미리보기 + HTML 복사/다운로드 |

공통 셸(전역 드롭존의 파일 타입 라우팅, 세션 통계, 한/영, 라이트/다크)도 전부 순수 프런트엔드로 가능하다.

### 2.1 기술 리스크와 한계

| 리스크 | 내용 | 대응 |
|--------|------|------|
| 대용량 PDF 메모리 | 수백 MB PDF는 브라우저 메모리에서 병합 시 탭 크래시 가능 | 파일당 100MB 소프트 경고, 진행 표시, 실패 시 안내 문구 |
| Safari WEBP 인코딩 | `toBlob("image/webp")`가 PNG로 폴백됨 | 결과 MIME 검사 후 "이 브라우저는 WEBP 저장 미지원 → JPG/PNG 권장" 안내. 필요 시 `@jsquash/webp`(wasm) 2차 도입 |
| `ctx.filter` 미지원 브라우저 | 구형 Safari | `getImageData` 픽셀 연산 폴백(흑백·세피아·밝기는 수식 단순) |
| CDN 의존 | pdf.js worker 등 CDN 단절 시 전체 불능 | 라이브러리를 저장소 `vendor/`에 동봉(정적 배포이므로 용량 부담만 확인) |
| URL 단축 CORS | qrlink API가 타 도메인 호출을 막으면 실패 | qrlink `api/`에 ax-demos 도메인 허용 헤더 1줄 추가 |

---

## 3. 사용자와 시나리오

- **본인(강사)**: 강의자료 PDF 병합·추출, 슬라이드 캡처 이미지 압축, 워크숍 QR/단축링크 생성: 일상 업무 도구.
- **워크숍 수강생**: "한 문장 프롬프트로 이런 앱이 나온다"를 보여주는 시연 앱. 도구 하나하나가 바이브코딩 실습 예제로 재사용된다.
- **일반 방문자**: 업로드 없는 파일 도구: 개인정보·보안 문서도 안심하고 처리.

핵심 시나리오: 첫 화면 전역 드롭존에 아무 파일이나 떨어뜨리면 확장자를 보고 알맞은 도구로 안내한다(이미지 → 편집 선택, PDF 여러 개 → 병합, JSON/CSV → 변환기, MD → 변환기).

---

## 4. 정보 구조와 아키텍처

### 4.1 구조 결정: "노빌드 멀티페이지 + 공통 모듈"

ax-demos의 단일 HTML 철학을 유지하되, 13종을 한 파일에 넣으면 유지보수가 무너지므로 **도구별 HTML + 공통 JS/CSS 모듈**의 정적 멀티페이지로 간다. 빌드 도구(Vite 등)는 도입하지 않는다(ES modules를 정적 서빙).

```
ax-demos/
  index.html              # 허브(기존): studio 진입 카드 추가
  studio/
    index.html            # 스튜디오 홈: 히어로 + 전역 드롭존 + 통계 + 도구 그리드
    pdf-merge.html        # 기존 pdf.html 이식 (딥링크 유지: pdf.html → 리다이렉트)
    pdf-split.html
    pdf-to-image.html
    image-to-pdf.html
    img-resize.html
    img-compress.html
    img-convert.html
    img-crop.html
    img-rotate.html
    img-filter.html
    url-short.html
    json-csv.html
    md-html.html
    assets/
      studio.css          # 디자인 토큰 + 공통 레이아웃(사이드바·툴바·드롭존)
      shell.js            # 사이드바 주입, 테마·언어 토글, 통계 기록
      file-router.js      # 전역 드롭존: 확장자 → 도구 라우팅
      stats.js            # sessionStorage 기반 처리 건수·절약 용량 집계
      i18n.js             # ko/en 문자열 사전
      canvas-utils.js     # 이미지 로드·리사이즈·내보내기 공통 함수
      pdf-utils.js        # pdf-lib/pdf.js 로드·페이지 썸네일 공통 함수
    vendor/               # pdf-lib, pdf.js(+worker), marked, DOMPurify, PapaParse, JSZip
```

- 각 도구 페이지는 `studio.css + shell.js + (도구 js 인라인)`만 물고, 도구 간 결합은 없다: 한 페이지가 깨져도 나머지는 산다.
- 상태 공유는 sessionStorage(통계·테마·언어)만. 파일 데이터는 페이지를 넘기지 않는다(도구 간 파일 전달은 비목표).

### 4.2 디자인

- `pdf.html`의 다크 토큰을 `studio.css`로 승격, 라이트 테마 변수 세트 추가(`:root[data-theme="light"]`).
- 레이아웃은 딸깍 스튜디오 구조를 차용: 좌측 카테고리 사이드바(이미지/PDF/문서) + 우측 작업 패널 + 상단 히어로·드롭존.
- 이모지 사용 금지(프로젝트 규율). 아이콘은 인라인 SVG.

---

## 5. 기능 요구사항 (도구별 상세)

각 도구 공통: 드래그앤드롭 + 파일선택 버튼, 처리 중 진행 표시, 결과 미리보기, 다운로드, "서버 전송 없음" 문구, 실패 시 원인 문구(형식 미지원/용량/브라우저 한계).

### 5.1 PDF 군

**T1. PDF 병합·페이지 편집** (기존 이식)
- 현행 기능 동일: 다중 불러오기, 썸네일, 삭제·회전·이동, 병합 저장.
- 추가: 파일 단위 블록 표시(어느 원본에서 온 페이지인지 색 태그), 드래그로 페이지 이동(포인터 이벤트, 버튼 방식은 유지).
- 수용 기준: 3개 PDF 30페이지 병합 결과가 순서·회전 반영 상태로 열림. 기존 pdf.html과 동일 결과.

**T2. PDF 분할·추출**
- 입력: PDF 1개. 썸네일 그리드에서 클릭 선택 + 범위 입력(`1-3,7,10-끝`).
- 출력 모드: (a) 선택 페이지만 1개 PDF, (b) N페이지 단위 분할 → zip, (c) 전 페이지 개별 PDF → zip.
- 수용 기준: 10페이지 PDF에서 `2-4,8` 추출 시 4페이지 PDF 생성. zip 모드에서 파일명 `원본명_p001.pdf` 규칙.

**T3. PDF → 이미지**
- 입력: PDF 1개. 옵션: 형식(PNG/JPG), 배율(1x=72dpi 기준, 2x, 3x), 페이지 범위.
- 출력: 1페이지면 단일 파일, 다중이면 zip.
- 수용 기준: 2x PNG로 뽑은 이미지의 픽셀 크기가 pdf.js viewport 계산과 일치.

**T4. 이미지 → PDF**
- 입력: JPG/PNG/WEBP 다중. 순서 변경 UI(T1의 카드 UI 재사용).
- 옵션: 용지(A4 세로/가로/원본 크기), 여백(없음/좁게/보통), 한 장당 1이미지.
- 수용 기준: 세로·가로 혼합 이미지 5장이 A4에서 비율 왜곡 없이 중앙 배치.
- 구현 주의: WEBP는 pdf-lib가 직접 embed 못 함 → canvas로 PNG 변환 후 embed.

### 5.2 이미지 군

**T5. 리사이즈**: 픽셀 지정(가로·세로, 비율 고정 토글) + 퍼센트 프리셋(25/50/75). 출력 형식은 원본 유지.
- 수용 기준: 4000px 사진 → 1200px 지정 시 종횡비 유지, EXIF 회전 반영(`createImageBitmap(file, {imageOrientation:"from-image"})`).

**T6. 압축**: 품질 슬라이더(0.3~0.95), 목표 형식(JPG/WEBP), before/after 용량·절약률 표시.
- 수용 기준: 5MB JPG를 품질 0.7로 재인코딩 시 용량 감소 표시가 실제 blob.size와 일치. PNG 입력 → JPG 출력 시 투명영역 흰색 합성.

**T7. 형식 변환**: JPG/PNG/WEBP 상호 변환, 다중 파일 일괄 처리(+zip).
- 수용 기준: Safari에서 WEBP 출력 선택 시 미지원 안내 노출(2.1 폴백 정책).

**T8. 자르기**: 캔버스 위 드래그 크롭 박스(핸들 8개), 비율 프리셋, 좌표·크기 수치 표시.
- 수용 기준: 크롭 결과 픽셀 크기가 표시 수치와 일치. 모바일 터치 동작.

**T9. 회전·뒤집기**: 90° 좌/우, 180°, 좌우·상하 반전, 실시간 미리보기.
- 수용 기준: 90° 회전 시 가로세로 치수 교환, 무손실 아님을 인지시키는 형식 안내(JPG 재인코딩).

**T10. 보정·필터**: 슬라이더: 밝기·대비·채도(각 0~200%), 프리셋: 흑백·세피아·선명하게. 원본 대비 토글.
- 수용 기준: 슬라이더 이동 시 300ms 이내 미리보기 갱신(미리보기는 축소본에 적용, 저장 시 원본 해상도 적용).

### 5.3 문서·텍스트 군

**T11. URL 단축**: qrlink-studio API 연동(POST /api/shorten 계약 확인 후 확정). QR 이미지 동시 표시(qrlink 클라이언트 코드 재사용). 복사 버튼.
- 수용 기준: 단축 URL 접속 시 원본으로 리다이렉트. API 실패 시 "QR만 생성" 폴백.
- 선행 조건: qrlink `api/`의 CORS 허용 도메인에 ax-demos 배포 도메인 추가(1줄 PR).

**T12. JSON ⇄ CSV**: 양방향. 좌측 입력(붙여넣기 + 파일), 우측 결과, 방향 자동 감지 + 수동 토글.
- JSON→CSV: 객체 배열 기준, 중첩은 `a.b` 평탄화, 배열 값은 JSON 문자열로.
- CSV→JSON: 헤더 필수, 숫자·불리언 자동 형변환 토글, 인코딩은 UTF-8(+ EUC-KR 감지 안내).
- 수용 기준: 한글·쉼표·따옴표·줄바꿈 포함 셀의 round-trip 무손실(PapaParse RFC4180).

**T13. Markdown → HTML**: 좌 편집기 / 우 미리보기 분할, GFM(표·체크박스·코드블록), 출력: HTML 복사(본문만/완전한 문서), .html 다운로드.
- 수용 기준: 표·코드블록 렌더 정상, DOMPurify로 script 제거(XSS 차단).

### 5.4 공통 셸

**S1. 스튜디오 홈**: 히어로 + 전역 드롭존 + 도구 그리드 + 세션 통계 4종(처리 파일 수, 절약 용량, 사용 도구 수, 최근 작업 흐름).
**S2. 파일 라우터**: 드롭 파일 확장자 판정: 이미지 1장 → 도구 선택 시트, 이미지 여러 장 → "일괄 변환 / 이미지→PDF" 선택, PDF 1개 → "분할 / 이미지로" 선택, PDF 여러 개 → 병합, json/csv/md → 해당 변환기. 라우팅 시 파일을 다시 고르게 하지 않고 `sessionStorage`에 담을 수 없으므로 **같은 페이지 내 도구 패널 전환 방식이 필요한 홈 드롭존만은 SPA성 패널로 처리**하거나, 단순화하여 "도구 페이지로 이동 안내 + 재드롭"으로 1차 구현(Phase 5에서 고도화 판단).
**S3. 세션 통계**: sessionStorage 집계. 절약 용량은 (원본−결과) 합, 음수는 0 처리.
**S4. 테마**: 다크 기본, 라이트 토글, `prefers-color-scheme` 초기값.
**S5. 한/영**: `i18n.js` 사전 치환(`data-i18n` 속성). 기본 ko.

---

## 6. 구현 순서 (Phase 게이트 방식)

규율: **각 Phase의 수용 기준을 전부 통과하고 사용자 게이트 판정을 받기 전에는 다음 Phase에 착수하지 않는다.** 게이트 판정은 사용자 몫이다.

### Phase 0: 골격과 기존 기능 무손실 이식 (1~2일)

| 순서 | 작업 | 산출물 |
|------|------|--------|
| 0-1 | `studio/assets/studio.css`: pdf.html 토큰 승격 + 라이트 테마 변수 | css 1개 |
| 0-2 | `vendor/` 구성: pdf-lib·pdf.js(+worker)·marked·DOMPurify·PapaParse·JSZip 동봉, CDN 제거 | vendor 6종 |
| 0-3 | `shell.js`(사이드바·테마)· `stats.js` 뼈대 | js 2개 |
| 0-4 | `studio/index.html` 홈(도구 그리드는 전부 "준비 중" 배지로 시작) | 홈 1개 |
| 0-5 | `pdf.html` → `studio/pdf-merge.html` 이식(공통 모듈 사용으로 재작성), 구 경로는 meta refresh 리다이렉트 | 도구 1종 |
| 0-6 | Vercel 배포 확인(정적 경로) | 배포 URL |

- **게이트 G0**: 배포 URL에서 병합 도구가 기존 pdf.html과 동일하게 동작(3파일 병합·회전·삭제·순서). 오프라인(CDN 차단)에서도 동작. 다크/라이트 전환 정상.

### Phase 1: PDF 군 완성 (2~3일)

| 순서 | 작업 | 근거 |
|------|------|------|
| 1-1 | `pdf-utils.js`: 파일→썸네일 그리드 공통화(병합 도구에서 추출) | T2·T3가 재사용 |
| 1-2 | T2 분할·추출(범위 파서 → 단일 PDF → zip 모드 순으로) | 병합의 역연산, 가장 검증 쉬움 |
| 1-3 | T3 PDF→이미지(단일 페이지 → 다중 zip 순) | pdf.js 렌더는 이미 보유 |
| 1-4 | T4 이미지→PDF(A4 배치 계산 → 순서 UI) | pdf-lib embed 신규 학습 구간 |

- **게이트 G1**: 실사용 파일 3종(강의 PDF, 스캔 PDF, 사진 5장)으로 T2·T3·T4 수용 기준 통과. 100MB 초과 경고 동작.

### Phase 2: 이미지 군 (3~4일)

| 순서 | 작업 | 근거 |
|------|------|------|
| 2-1 | `canvas-utils.js`: 로드(EXIF 방향)·내보내기(toBlob, WEBP 폴백 감지)·용량 표시 공통화 | 6종 전체의 토대. 최우선 |
| 2-2 | T5 리사이즈 → T9 회전·뒤집기 → T7 형식 변환 | 셋 다 canvas-utils 직결, UI 단순 |
| 2-3 | T6 압축(품질 슬라이더 + before/after) | toBlob quality 활용 |
| 2-4 | T10 보정·필터(ctx.filter + 픽셀 폴백) | 미리보기 성능 작업 포함 |
| 2-5 | T8 자르기(크롭 박스 UI: 이미지 군 중 유일한 복합 UI라 마지막) | 터치 대응 포함 |

- **게이트 G2**: iPhone 사진(EXIF 회전)·투명 PNG·대형 4000px 이미지 3종으로 6개 도구 수용 기준 통과. Safari에서 WEBP 안내 확인.

### Phase 3: 문서·텍스트 군 (2일)

| 순서 | 작업 | 근거 |
|------|------|------|
| 3-1 | T13 Markdown→HTML(marked+DOMPurify, 로컬 완결) | 서버 의존 없음, 반나절 |
| 3-2 | T12 JSON⇄CSV(PapaParse, round-trip 테스트 셋 동봉) | 로컬 완결 |
| 3-3 | qrlink-studio API 계약 확인 + CORS 1줄 수정·배포 | T11 선행 조건 |
| 3-4 | T11 URL 단축(+QR 표시, API 실패 폴백) | 유일한 외부 연동 |

- **게이트 G3**: 한글 CSV round-trip 무손실, XSS 마크다운 차단, 단축 URL 리다이렉트 실측.

### Phase 4: 홈·통계·라우터·다국어 (2일)

| 순서 | 작업 |
|------|------|
| 4-1 | S3 세션 통계를 13종 도구에 배선, 홈 통계 카드 활성화 |
| 4-2 | S2 파일 라우터 1차(도구 안내 방식) |
| 4-3 | S5 한/영 사전 작성·적용 |
| 4-4 | 홈 "준비 중" 배지 전부 제거, README 갱신 |

- **게이트 G4**: 첫 방문 → 드롭 → 안내 → 처리 → 통계 반영의 전체 흐름 시연. 모바일 뷰 확인.

### Phase 5: 선택 고도화 (착수 여부 자체를 게이트로)

- 라우터 SPA화(재드롭 없이 파일 전달), wasm WEBP 인코더, PDF 압축(이미지 다운샘플), 도구 간 체이닝(자르기 결과 → 압축), PWA 오프라인.

**총 예상 규모: Phase 0~4 약 10~13 작업일** (하루 2~3시간 바이브코딩 기준 3~4주).

---

## 7. 검증 계획

- 도구별 수용 기준(5장)을 체크리스트로 삼아 Phase 게이트마다 실파일 검증.
- 브라우저 매트릭스: Chrome(주) / Safari(macOS·iOS) / Edge. Safari가 제약의 대부분(WEBP·filter)이므로 G2는 Safari 필수.
- 회귀 최소선: 병합 도구는 매 Phase 배포 후 3파일 병합 스모크 테스트(기존 사용자 흐름 보호).
- 테스트 픽스처를 `studio/test-fixtures/`에 동봉(작은 PDF 2종, EXIF 사진, 한글 CSV, XSS md).

## 8. 배포·운영

- 현행 Vercel 정적 배포 그대로(빌드 스텝 없음). `studio/` 하위 추가만으로 URL 확정.
- vendor 동봉으로 저장소 용량 +3~4MB 예상(pdf.js가 대부분): 정적 배포 한도 내 무해.
- 유일한 운영 의존: qrlink-studio의 Supabase(이미 운영 중). 장애 시 URL 단축만 폴백 동작.

## 9. 열린 결정 (사용자 판정 필요)

1. ~~스튜디오 이름·브랜딩~~ → **내PC 스튜디오**로 확정(2026-07-19).
2. Phase 5 착수 여부와 항목: G4 통과 후.
3. qrlink 단축 도메인을 그대로 쓸지, ax-demos 도메인 경유로 감쌀지: Phase 3 전.
